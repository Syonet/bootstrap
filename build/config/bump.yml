options:
  files:
    - package.json

  updateConfigs:
    - package

  commit: true
  commitMessage: "chore(release) versão %VERSION%"
  commitFiles:
    - package.json
    - dist/**/*

  createTag: true
  tagName: "%VERSION%"
  tagMessage: "Release %VERSION%"

  push: true
  pushTo: origin