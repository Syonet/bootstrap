options:
  files:
    - package.json

  updateConfigs:
    - package

  commit: true
  commitMessage: "Release %VERSION%"
  commitFiles:
    - package.json
    - dist/**/*

  createTag: true
  tagName: "%VERSION%"
  tagMessage: "Release %VERSION%"

  push: true
  pushTo: origin